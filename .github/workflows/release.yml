name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      publish_target:
        description: "Upload to TestPyPI (configure Trusted Publishing for environment testpypi). Tag pushes upload to PyPI."
        type: choice
        default: none
        options:
          - none
          - testpypi

permissions:
  contents: read

jobs:
  linux:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v6
      - uses: PyO3/maturin-action@v1
        with:
          command: build
          target: ${{ matrix.target }}
          args: >-
            --release --out dist
            -i python3.9 -i python3.10 -i python3.11
            -i python3.12 -i python3.13 -i python3.14
          manylinux: "2_28"
          sdist: ${{ matrix.target == 'x86_64' }}
          artifact-name: wheels-linux-${{ matrix.target }}-2_28

  linux-manylinux2014:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v6
      - uses: PyO3/maturin-action@v1
        with:
          command: build
          target: ${{ matrix.target }}
          args: >-
            --release --out dist
            -i python3.9 -i python3.10 -i python3.11
            -i python3.12 -i python3.13
          manylinux: auto
          sdist: false
          artifact-name: wheels-linux-${{ matrix.target }}-2014

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.9"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.10"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - uses: PyO3/maturin-action@v1
        with:
          command: build
          args: >-
            --release --out dist
            -i python3.9 -i python3.10 -i python3.11
            -i python3.12 -i python3.13 -i python3.14
          artifact-name: wheels-windows-x64

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.9"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.10"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - uses: PyO3/maturin-action@v1
        with:
          command: build
          args: >-
            --release --out dist
            -i python3.9 -i python3.10 -i python3.11
            -i python3.12 -i python3.13 -i python3.14
            --target universal2-apple-darwin
          artifact-name: wheels-macos-universal2

  collect:
    name: Check distributions
    needs: [linux, linux-manylinux2014, windows, macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: actions/download-artifact@v8
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true
      - name: Twine check
        run: |
          python -m pip install --upgrade pip twine
          python -m twine check dist/*

  publish-pypi:
    name: Publish to PyPI
    needs: [collect]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true
      - uses: pypa/gh-action-pypi-publish@release/v1

  publish-testpypi:
    name: Publish to TestPyPI
    needs: [collect]
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.publish_target == 'testpypi'
    environment: testpypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

  github-release:
    name: Upload release assets
    needs: [collect]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true
      - uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          tag_name: ${{ github.ref_name }}
